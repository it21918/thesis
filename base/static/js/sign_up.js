document.getElementById('sign_up').addEventListener('click', (event) => {

    // var inputs = document.getElementsByTagName('input');
    // var p = document.getElementsByTagName('p');
    // var button = document.getElementsByTagName('button')
    // var img = document.getElementsByTagName('img')
    // while (inputs.length) inputs[0].parentNode.removeChild(inputs[0]);
    // while (p.length) p[0].parentNode.removeChild(p[0]);
    // while (button.length) button[0].parentNode.removeChild(button[0]);
    // while (img.length) img[0].parentNode.removeChild(img[0]);
    
})
